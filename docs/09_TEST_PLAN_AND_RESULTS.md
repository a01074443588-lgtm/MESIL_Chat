# 시험계획과 결과

시험일: 2026-07-26
자료: 가상 조직·가상 직원만 사용

## 자동화 시험

`backend/tests/test_core_flow.py`, `backend/tests/test_auth_security.py`, `backend/tests/test_workdesk_flow.py`에서 검증했다.

- 관리자 로그인·조직 생성·직원 2명 등록
- 전체·사업부·부서·직종·층·팀 자동방
- 지정방
- 관리자 전체 개설방 목록·생성·이름/참여자 변경·기록보존 종료·복구
- 일반 직원의 방 관리 API `403`
- 자동방과 직접 선택 방 모두 종료 후 `rooms_changed`와 기존 접근 `403`
- 두 독립 세션과 WebSocket 실시간 수신
- 읽지 않은 수·읽음 처리
- 층·팀 변경 후 이전 방 `403`
- 권한 없는 다른 직원의 새 방 `403`
- 퇴사 WebSocket `force_logout`
- 기존 API 세션 차단·재로그인 `403`
- 앱 수명주기 재시작 후 방·메시지 유지
- 새 직원의 임시 비밀번호 상태에서 채팅 API `403`
- 비밀번호 변경 후 채팅 허용
- 비밀번호 변경 시 다른 세션·WebSocket 종료
- 로그인 기기 개별 종료와 기존 API 세션 `401`
- 관리자 초기화 후 모든 세션·WebSocket 종료
- 로그인 5회 실패 후 다음 요청 `429`와 `Retry-After`
- 층별 가상 어르신 목록, 사진 첨부, 읽음 명단, 댓글
- 사용자별 새 댓글 수와 댓글 확인 처리
- 직접 선택 방의 참여자 배정과 어르신 우선층을 서로 다르게 설정
- 보내기 단계의 인수인계·업무협조·확인요청 및 담당자 지정
- 업무함의 방·기간 단위 대화·댓글·어르신·업무지정 집계
- 업무자료 처리함 생성·상태변경·권한검사
- 원문 스냅샷과 사진 ID 보존
- 시험 제안 전 확정상태 변경 차단
- 시험 제안의 분류·위험도·전달대상·서류 후보 생성
- 제안과 담당자 확정 결과 분리 및 확정자·확정시각 저장
- 일반 직원의 제안·확정 API 접근 차단
- 확정된 자료만 서류 후보 대시보드에 집계
- 서류 종류·업무 분류·위험도 필터
- 확정 전 자료와 일반 직원의 후보조회 차단
- 매 시험마다 독립 PostgreSQL 스키마 생성·폐기

결과: `3 passed`. FastAPI 시험도구 내부의 향후 변경 예정 경고 1건이 있으며 제품 실패는 아니다.

추가 검증:

- 방 밖 가상 간호조무사가 담당자 후보에서 제외됨
- 방 밖 직원 ID를 메시지 API에 직접 넣어도 `422`
- 현재 방 참여 직원은 담당자 후보에 유지됨
- 프런트엔드 빌드·렌더링 시험 2개·정적검사 통과

## 업무지정·공통 UI 브라우저 시험

- 1366×768에서 `업무 종류 / 담당 직원 / 담당 팀 / 중요도` 네 항목이 잘리지 않고 표시됨
- 전체 직원방에 실제 참여 중인 간호조무사가 담당 직원 목록에 표시됨
- 업무협조 전송 후 말풍선에 `업무지정 · 업무협조 · 담당자 · 미확인` 표시
- 크게 보기 제목이 `업무지정 상세 / 업무협조`로 표시되고 같은 담당정보를 재확인 가능
- 컨테이너 재시작 후 시험 메시지와 업무지정 정보 유지
- 390×844에서 업무지정 배지·입력부·보내기 버튼이 가로 넘침 없이 표시됨
- 로그인과 채팅 상단에 SMCODI에서 사용하는 실버메디컬 원본 로고 적용

## 이번 업그레이드 실제 브라우저 시험

- 관리자 `현재 개설방`에서 시험방 생성 → 종료 → 목록 즉시 제거 → 종료방 표시 → 복구 → 재종료
- 종료 후 참여자 방 목록이 4개에서 3개로 즉시 변경되고 기존 기록 보관 안내 표시
- `대화방 요약 / 처리할 대화 / 인수인계·업무 / 서류 후보` 업무함 탭 확인
- 오늘 기준 방별 대화·댓글·어르신 관련·업무지정 수 집계와 원문 묶음 확인
- 메시지에 `댓글 2 · 새 댓글 1`, `댓글 1` 표시 확인
- 채팅 입력부의 `어르신 선택 / 사진 / 공지 / 업무로 보내기` 확인
- 직접 선택 방식 `3층방`에 별도 어르신 우선층 `3층` 지정
- `3층방` 어르신 목록에서 3층 가상 어르신 13명이 2·4·5층보다 먼저 표시됨을 확인
- 390×844 화면에서 메시지 크게 보기 안내, 댓글 배지, 공지, 입력 버튼이 겹치지 않음
- 화면 캡처: `output/playwright/smcodi-upgrade-mobile.png`
- 브라우저 시험 중 PostgreSQL 세션 검색경로가 간헐적으로 해제되는 문제를 발견해 연결단위 설정으로 수정
- 수정·재빌드 뒤 최근 백엔드 로그에서 `500`, `UndefinedTable`, `Traceback` 없음 확인

## 실제 브라우저 시험

- 제출용 요양보호사 역할 2개를 독립 Chrome 세션으로 로그인
- 두 사용자가 `3층 직원방`을 연 상태에서 보고 작성자 역할이 메시지 작성
- 실시간 수신자 역할 화면에 새로고침 없이 작성자·본문·시간 표시
- 서버 프로세스 재시작 후 로그인 세션·직원·방·메시지 유지
- 관리자가 보고 작성자 역할을 3층에서 4층으로 변경
- 해당 역할 화면에서 3층방 제거·4층방 추가
- 서버 API에서 이전 3층방 직접 접근 시 `403`
- 관리자가 보고 작성자 역할을 퇴사 처리
- 열려 있던 해당 역할 화면에 강제종료 알림 후 로그인 화면 표시
- 같은 계정 재로그인 시 “퇴사 처리되어 로그인할 수 없습니다.”
- 메시지를 크게 열고 읽은 직원 2명과 댓글 1건을 양쪽 화면에서 확인
- 3층방 어르신 선택목록에 가상 어르신 13명 표시
- 소속 변경 직후 이미 열려 있던 이전 방 상세화면도 닫히도록 보완
- 보고 작성자 역할이 3층 가상 어르신을 선택해 식사량 감소 메시지 작성
- 업무함 검토자 역할에서 보존 원문 확인
- `prototype-rule-v1`이 식사·영양, 관찰 필요, 간호·사회복지 전달대상으로 제안
- 사회복지사가 요약과 검토메모를 수정해 담당자 확정
- DB에서 원문·시험 제안·담당자 확정문이 각각 분리 저장됨을 확인
- 390×844 모바일 화면에서 세 단계와 확정결과 표시 확인
- 사회복지사 업무함에서 `처리할 대화 / 서류 후보` 전환 확인
- 확정 후보 1건, 높음·긴급 0건 집계 확인
- 급여제공기록지·통합사정 각 1건, 간호일지 0건 표시 확인
- 간호일지 필터 0건 → 급여제공기록지 필터 1건 전환 확인
- 후보 상세에서 확정문·확정자·채팅원문·공식서류 미반영 안내 확인
- 390×844 모바일 화면에서 집계·필터·상세 추적정보 확인
- 관리자가 가상 직원 2명을 선택해 `브라우저 검증 임시방` 생성
- 참여 직원 검색에 `3층` 입력 시 해당 층 요양보호사 6명만 표시
- 3층 요양보호사 화면에 지정방이 새로고침 없이 추가됨
- 관리자가 참여자에서 3층 요양보호사를 제외하자 방 수가 7개에서 6개로 즉시 변경
- 방 이름 변경 후 남은 사회복지사 화면에 수정 이름 표시
- 관리자가 지정방 종료 후 사회복지사 화면의 방 수가 6개에서 5개로 즉시 변경
- 390×844 모바일 화면에서 수정된 지정방과 종료 후 목록 확인
- 종료된 방은 비활성화하고 기존 대화·감사기록은 데이터베이스에 보관
- 컨테이너 재시작 후 지정방 전체 1건·활성 0건·종료 1건으로 기록 유지 확인

## 로그인 보안 브라우저 시험

- 관리자가 `uiworker`의 임시 비밀번호를 재발급
- 열려 있던 직원 브라우저에 초기화 알림 후 즉시 로그인 화면 표시
- 임시 비밀번호로 로그인하면 채팅방 대신 강제 변경화면 표시
- 새 비밀번호 변경 후 기존 6개 채팅방 정상 표시
- 같은 직원으로 두 번째 독립 Chrome 세션 로그인
- 첫 번째 브라우저 보안 설정에서 로그인 기기 2개와 현재 기기 구분 표시
- 두 번째 기기 종료 시 해당 브라우저 WebSocket 알림 후 로그인 화면 표시
- 실제 Docker API에서 5회 `401`, 6번째 `429`, `Retry-After: 900` 확인

## DB·배포 시험

- PostgreSQL 빈 시험 스키마에 Alembic 기준 마이그레이션 적용
- `alembic current`가 `005_resident_scope_unit (head)`임을 확인
- `alembic check`에서 미반영 모델 변경 없음 확인
- UUID·`timestamptz`·JSONB 실제 열 형식 확인
- PostgreSQL custom 백업 생성과 `pg_restore --list` 판독 확인
- 프런트엔드 빌드 성공
- 프런트엔드 테스트 `2 passed`, 정적검사 성공
- 백엔드 프로세스 재시작 전후 `직원 77 / 조직 29 / 방 42 / 메시지 7 / 댓글 4 / 처리항목 3 / 업무지정 0` 동일 확인
- 현재 코드로 백엔드·프런트엔드 Docker 이미지 재빌드 후 `8080` 화면·API·PWA 매니페스트 응답 `200`

## PWA 시험

- 390×844 모바일 화면 캡처
- 192·512 아이콘 응답 200
- 매니페스트 응답 `application/manifest+json` 확인
- 매니페스트 오류 0
- 설치가능성 오류 0
- 서비스워커가 페이지를 제어

## 미수행

- 실제 휴대전화 잠금화면에서 Web Push 도착·시스템 알림음·진동 물리기기 시험
- 다중 백엔드 서버
- 실제 SMCODI DB 연동 또는 데이터 이전

## 2026-07-27 화면 꺼짐 Web Push·의료형 알림음

- 로그인 세션에 연결되는 Web Push 구독 저장·조회·해제 API 구현
- 로그아웃된 세션의 구독에는 푸시를 보내지 않는 자동시험 추가
- 텍스트·파일·전달 메시지 저장 후 발신자를 제외한 참여자에게 백그라운드 푸시를 예약
- 푸시 실패가 채팅 저장을 되돌리거나 전송을 지연하지 않도록 백그라운드 처리
- 잠금화면 알림문을 `새 메시지가 도착했습니다.`로 고정하여 메시지·직원·방·어르신 정보 미포함
- 서비스워커 v5에서 화면이 보일 때 중복 시스템 알림을 생략하고, 백그라운드에서만 시스템 알림 표시
- 알림 클릭 시 기존 MESIL_Chat 창을 우선 열고 없으면 새 창을 여는 동작 구현
- 전면 앱 알림음을 523.25Hz → 659.25Hz → 783.99Hz의 부드러운 3음으로 변경
- 공개 `https://chat.silvermedical.kr/`에서 `알림 설정`, `휴대전화 알림 켜기`, `3음 시험 소리 듣기` 노출 확인
- 공개 서비스워커 v5 응답 `200`, 캐시 우회, 서비스워커 허용범위 `/` 확인
- PostgreSQL 마이그레이션 `017_web_push_subscriptions (head)` 및 미반영 모델 변경 없음 확인
- 최종 전체검증: 백엔드 `23 passed`, 프런트 렌더링 `2 passed`, 빌드·린트 통과

### 물리기기 확인이 남은 항목

1. 휴대전화 MESIL_Chat에서 `알림 설정 → 휴대전화 알림 켜기`를 누른다.
2. `시험 알림 보내기`로 잠금화면 표시·시스템 소리·진동을 확인한다.
3. 화면을 끄고 PC의 다른 직원 계정에서 메시지를 보내 즉시 도착하는지 확인한다.
4. 휴대전화에서 메시지를 열어 누락 없이 동기화되는지 확인한다.

### 첫 물리기기 시험에서 발견한 오류와 수정

- 휴대전화 2개의 FCM 구독이 서버에 정상 저장되고 시험 전송 요청이 `202`로 접수되는 것 확인
- 실제 전송은 VAPID 발신자 주소 `https://chat.silvermedical.kr/` 끝의 `/` 때문에 서명검증 전에 실패
- 발신자 주소를 `https://chat.silvermedical.kr`로 정규화하고 서버 재배포
- 시험 전송을 동기 확인하도록 바꿔 실제 전송이 0건이면 더 이상 성공 안내를 표시하지 않도록 수정
- 시험 알림은 설정 화면이 열려 있어도 시스템 알림을 표시하여 버튼 동작을 바로 확인하도록 수정
- 휴대전화에 남은 이전 알림 처리기를 확실히 교체하도록 서비스워커 v6와 활성화 대기 적용
- 개인정보와 푸시 주소를 출력하지 않는 실패유형·상태코드 서버 경고기록 추가
- 수정 후 FCM 전송 성공 1건, 해당 구독 실패횟수 `4 → 0`, 최초 성공시각 저장 확인
- 수정 후 전체검증: 백엔드 `23 passed`, 프런트 렌더링 `2 passed`, 빌드·린트·마이그레이션 일치검사 통과

## 2026-07-26 관리자 화면 사용성 회귀시험

- 직원 77명을 목록과 상세 편집 영역으로 분리하고 이름·아이디·소속·직종 검색을 확인
- `시설 사회복지사` 검색 시 77명 중 3명만 표시되는 것을 실제 브라우저에서 확인
- 직원 상태 배지를 한 줄 우선 자동 줄바꿈으로 바꿔 목록 카드의 잘림을 제거
- 조직·직종마다 현재 재직 직원 수와 활성 채팅방 수를 표시
- 직원 또는 활성방에서 사용하는 조직·직종의 `중지` 버튼이 `사용 중` 비활성 상태가 되는 것을 확인
- 사용 중 조직의 중지 API가 `409`로 거부되는 자동화 시험 통과
- 채팅방 42개를 검색·배정방식 필터가 있는 목록과 상세 관리 영역으로 분리
- 같은 이름의 `방문요양 전체방` 2개가 `사업부 자동 · 방문요양`, `부서 자동 · 방문요양`으로 구분되는 것을 확인
- 위 두 방을 검색한 뒤 사업부 필터 적용 시 1개만 남는 것을 확인
- 390×844 화면에서 직원 선택 시 상세 편집영역으로 이동하고, 관리 탭 변경 시 새 탭 상단으로 복귀하는 것을 확인
- 업무지정 모드의 최종 버튼이 `업무협조 전송` 접근성 이름과 `전송` 표시문구를 사용하는 것을 확인
- Docker 재빌드 후 API 상태 응답 정상 확인

화면 캡처:

- `output/playwright/admin-improved-employee-detail-desktop-fixed.png`
- `output/playwright/admin-improved-organization-desktop.png`
- `output/playwright/admin-improved-rooms-desktop.png`
- `output/playwright/admin-improved-employee-detail-mobile.png`
- `output/playwright/admin-improved-tab-scroll-reset-mobile.png`
- `output/playwright/chat-work-send-label-mobile.png`

## 2026-07-26 파일 첨부·로컬 OCR 시험

- 전송 도구의 `사진`을 `파일`로 변경하고 JPG·PNG·WEBP, MP3·WAV·M4A·WEBM·OGG·AAC, PDF 허용을 확인
- 파일 내용 시그니처와 표시된 MIME 형식이 다르면 API가 `422`로 거부하도록 확인
- WAV 파일만 본문 없이 전송했을 때 기본본문 `파일을 첨부했습니다.`와 `audio/wav` 메타데이터 저장 확인
- 보고서 이미지 선택 시 어르신을 고르지 않아도 채팅 메시지·이미지가 먼저 표시되고, 업무함에는 `어르신 확인 필요` 상태로 들어오는 것을 실제 브라우저에서 확인
- `attachment_text_extractions`에 `pending → processing → completed → reviewed` 상태가 저장되는 것을 확인
- 자동시험에서는 결정론적 시험 OCR로 판독문 생성·담당자 수정·일반직원 접근 `403`을 확인
- 실제 Docker 백엔드가 `host.docker.internal:11434`의 로컬 Ollama에 연결하는 것을 확인
- Qwen3-VL 기본 문맥 262,144에서 46GB·CPU/GPU 혼합·180초 시간초과를 재현하고, 문맥 8,192로 제한 후 6.5GB·100% GPU로 조정
- 실제 가명 화면 이미지에서 16.8초에 한글 268자·여러 줄 판독 결과 생성 확인
- 실제 브라우저 업무함에서 `로컬 판독 중 → 판독 완료 · 확인 필요` 전환, 판독문 편집, `담당자 확인 완료` 저장 확인
- 담당자 확인문이 시험 제안의 오타검토문·요약 입력에 다시 반영되는 것을 확인
- 카카오톡 내보내기 2개, 대화 2,205건에서 로컬 이름 후보 80개·업무용어 140개를 생성하고 결과 파일이 Git 제외되는 것을 확인
- 390×844 화면에서 첨부 원본·OCR 카드·판독문 입력이 한 열로 표시되는 것을 확인
- PostgreSQL Alembic 헤드 `006_attachment_text_extraction`, `alembic check` 미반영 변경 없음 확인
- 사용자가 제공한 실제 손글씨 사진 3장을 외부 전송 없이 로컬에서 예비시험
- 기본 `qwen3-vl:8b` 생각형은 4,096~8,192 출력 토큰을 생각 과정에 모두 사용해 세 장 모두 판독문이 비어 실패
- `gemma4:e2b`는 빠르게 응답했지만 심한 누락과 최대 98.3%의 행 반복이 발생해 채택하지 않음
- 공식 비사고형 `qwen3-vl:8b-instruct`로 변경하고 세로 사진을 3구간으로 나눈 결과 세 장 모두 12.68~15.46초에 판독문 생성
- 3구간 처리 후 행 반복률은 세 장 모두 0%였으나 성명·숫자·일부 단어의 오인식은 남아 담당자 원본 대조가 필수임을 확인
- 재빌드한 Docker 백엔드에서 새 모델·3구간 설정을 확인하고 기존 가명 시험 이미지를 4.42초, 431자, 58줄로 판독
- 백엔드 `6 passed`, 프런트엔드 빌드·테스트 `2 passed`·정적검사 통과

### 검증 한계

- 실제 손글씨 3장은 예비 적합성 확인용이며 개인정보가 포함될 수 있어 원본·판독문·요약은 Git 제외 로컬 `output`에만 저장했다.
- 정답 전사본이 없어 글자 오류율을 수치로 확정하지 않았다. 숫자·날짜·성명·투약 표현 오류율은 가명 손글씨와 정답 전사본 30~50장 비교 전까지 확정할 수 없다.
- 음성파일 업로드·메타데이터 저장은 자동시험했지만 음성인식은 이번 범위가 아니다.

## 2026-07-26 다중 어르신 자동분류·Nemotron 비교

- 일반 메시지에서 어르신을 선택하지 않고 서로 다른 가명 어르신 이름 두 개를 쓴 뒤 두 건 모두 `확인 후보`로 생성되는 것을 실제 브라우저에서 확인
- 담당자가 첫 후보만 `맞음` 처리한 상태에서는 시험 제안 생성이 차단되고, 나머지 후보를 확인하거나 제외하라는 API `422`를 확인
- 두 후보를 모두 `맞음` 처리한 뒤 업무기록 요약에 두 이름이 함께 표시되는 것을 확인
- 방·기간 요약도 대표 어르신 한 명이 아니라 확인된 여러 어르신을 모두 집계하도록 변경
- 어르신 미선택 상태에서 시험용 아이콘 이미지를 `보고서 이미지`로 전송한 뒤 업무함에 `처리 대기 · 어르신 확인 필요`로 들어오는 것을 실제 브라우저에서 확인
- 화면 새로고침 후 브라우저 콘솔 오류·경고 0건 확인
- 자동시험: 백엔드 6건, 프런트엔드 출력시험 2건, 빌드·정적검사 통과
- PostgreSQL 마이그레이션 `007_multi_resident_links (head)`, 모델과 마이그레이션 차이 없음 확인
- 제공된 손글씨 사진 12장을 외부 전송 없이 로컬에서 Qwen3-VL과 Nemotron OCR v2로 비교
- Nemotron OCR v2는 모델 준비 후 장당 약 0.11~0.14초, 최대 CUDA 할당 약 1.85GB로 매우 빨랐지만, 한국어 손글씨의 심한 누락·오인식과 사실상 빈 결과가 확인되어 주 판독기로 채택하지 않음
- Qwen3-VL 3구간 판독은 완료된 장에서 약 11.98~16.33초가 걸렸고 서술문과 이름을 더 많이 보존했지만, 12장 중 2장은 반복 방지 규칙으로 실패해 사람의 원본 대조가 계속 필요함

화면 캡처:

- `output/playwright/multi-resident-confirmed.png`
- `output/playwright/report-image-no-resident.png`

### 추가 검증 한계

- 12장에는 개인정보가 포함될 수 있어 원본과 판독문을 Git에 넣지 않았고, 비교 산출물은 Git 제외 로컬 `output`에만 저장했다.
- 정답 전사본이 없으므로 문자 오류율(CER)을 계산하지 않았다. 이번 비교는 누락·가독성·실패 여부를 본 예비 판정이다.
- 이름 자동찾기는 현재 정확히 일치하는 활성 어르신 이름만 사용한다. 비슷한 이름, 별칭, 동명이인은 자동 확정하지 않는다.

## 2026-07-26 기관어휘 진화·로컬 AI 최종검토

- 이름과 업무용어를 한 목록에서 50개까지만 읽던 제한을 제거하고 `어르신명 / 기관 대화 용어 / 장기요양 기본용어 / 승인 교정`으로 분리
- 제공된 카카오톡 2개에서 대화 2,205건, 이름 후보 115개, 기관용어 140개, 장기요양 기본용어 39개를 분리 생성
- 현재 채팅 DB 14건에서 채팅용어 47개를 별도 재집계하고 카카오 기관용어와 통합한 상위 240개 목록 생성
- 채팅 전송 경로와 용어 집계를 분리해 메시지 작성 시 추가 AI/OCR 지연이 없도록 확인
- 담당자가 확인한 샘플 오류 `고체 → 교체`, `락인 → 확인`, `속때르 → 속패드`가 이름 후보 수와 무관하게 표시되는 것을 실제 브라우저에서 확인
- `바지와 → 바지` 같은 어미 절단 오추천을 줄이기 위해 업무용어 유사추천은 같은 글자수·높은 유사도일 때만 표시
- 담당자 OCR 확인문에서 안전한 짧은 한글 단어 교정만 PostgreSQL에 반복횟수와 함께 저장하는 마이그레이션 적용
- SMCODI가 내보낸 JSON/CSV 어르신 명단을 읽어 Git 제외 로컬 명단으로 만드는 스크립트와 가명 예제 검증
- SMCODI 원본 DB·Carefor에는 접속하거나 쓰지 않았으며, 현재 어르신명은 읽기 전용 내보내기 파일로만 받도록 경계 유지
- 업무함의 `로컬 AI로 초안 다듬기` 버튼을 실제 Docker 화면에서 실행
- Ollama `qwen3-vl:8b-instruct`가 약 7초 안에 요약을 다듬고 `local-ai:qwen3-vl:8b-instruct` 생성기와 `ai_reviewed` 상태를 저장
- AI 실행 뒤에도 `담당자 확인·확정`이 별도로 남고 자동 서류전송이 되지 않는 것을 확인
- PostgreSQL Alembic `008_ocr_correction_memory (head)`, 미반영 모델 변경 없음
- 백엔드 8건, 프런트엔드 출력시험 2건, 빌드·정적검사 모두 통과
- Docker 재빌드 후 `http://localhost:8080/api/health` 정상

화면 캡처:

- `output/playwright/workdesk-local-ai-review.png`

### 검증 한계

- 로컬 AI는 초안 보조이며 이름·수치·위험표현의 정확성을 보장하지 않는다. 담당자가 원문과 사진을 대조해야 한다.
- SMCODI 실제 내보내기 형식은 아직 전달받지 않아 현재 스크립트는 JSON/CSV 공통 계약과 가명 예제로 검증했다.
- 교정기억은 안전을 위해 자동 치환하지 않는다. 반복횟수가 쌓여도 담당자 확인 없이 공식 기록으로 승격하지 않는다.

## 2026-07-27 관리자 승인형 어르신 동기화

- 관리자 전용 `어르신 동기화` 탭과 JSON/CSV 파일 입력을 구현
- 가명 명단의 신규·정보 변경·이용 중지·변경 없음·확인 필요 비교결과와 건수 표시
- 관리자가 선택한 항목만 승인·반영하고 선택하지 않은 항목은 보류하는 부분 승인 확인
- 존재하지 않는 층과 중복 외부 식별자를 `확인 필요`로 차단
- 빈 명단을 전체 이용중지로 오인하지 않도록 `422` 거부 확인
- 동기화 어르신만 `SMCODI:` 식별범위로 관리해 수동 등록 어르신이 파일 누락으로 중지되지 않는 자동시험 통과
- 이용중지는 물리 삭제가 아니라 `is_active=false`, `status=inactive`로 기록 유지 확인
- 미리보기와 반영마다 파일 요약값·현재 스냅샷·승인자·감사기록 저장 확인
- 실제 Docker 화면에서 가명 신규 2명 미리보기 → 전체 선택 → 승인·반영 → `어르신 순서` 노출 확인
- Docker 백엔드 재시작 뒤 신규 2명과 `반영 완료`, 적용 2건 이력 유지 확인
- 390×844 실제 브라우저 크기에서 관리 패널 390px, 내부 내용 390px로 가로 넘침 없음 확인
- PostgreSQL Alembic `009_resident_sync_batches (head)`, 모델과 마이그레이션 차이 없음 확인
- 신규 동기화 2건을 포함한 백엔드 자동시험 10건, 프런트엔드 출력시험 2건·빌드·정적검사 통과

### 검증 한계

- Chrome 확장 프로그램의 `로컬 파일 URL 접근 허용`이 꺼져 있어 브라우저 자동화로 파일 선택기를 통과하지 못했다. 동일 가명 파일의 실제 Docker API 업로드 후 화면 미리보기·승인 조작은 검증했다.
- SMCODI 실제 내보내기 파일은 아직 받지 않았으며 가명 공통계약으로 검증했다.
- 자동 주기 실행, SMCODI/Carefor 직접 연결, 실명자료는 이번 범위가 아니다.

## 2026-07-27 동기화 화면 단순화·연습모드

- 화면을 열 때 최신 완료기록이 자동으로 펼쳐지지 않고 `가명자료로 연습하기`가 첫 행동으로 보이는 것을 실제 Docker 브라우저에서 확인
- 가명 연습 미리보기에서 새 어르신 2명만 표시되고 기존 명단의 이용 중지 후보는 0명인 것을 확인
- 새로 추가·정보 바뀜은 기본 선택하고 이용 중지는 자동 선택하지 않는 안전규칙 적용
- `선택한 2명 저장하기` → `이 작업은 이미 끝났습니다` → `저장된 어르신 보기` 흐름을 실제 브라우저에서 끝까지 확인
- 동기화 화면의 안내문·라벨·요약·선택영역 글자와 연습 버튼 터치영역 확대
- 백엔드 자동시험 10건, 프런트엔드 출력시험 2건, 빌드·정적검사, PostgreSQL 마이그레이션 일치 검사 통과

## 2026-07-27 어르신 명단 화면 재단순화·OCR 반복오류 보완

- 관리자 메뉴 이름을 `어르신 명단 가져오기`로 바꾸고 화면 첫 부분에 용도와 현재 연결상태를 표시
- 실제 SMCODI 내보내기 연결 전에는 “실제 명단 파일이 없으면 할 일이 없다”는 안내가 먼저 보이는 것을 실제 Docker 브라우저에서 확인
- 가명자료 기능을 `개발·시험용 가명자료 보기` 안으로 접어 일반 업무 행동과 분리
- 같은 가명자료를 다시 비교했을 때 `0명 선택` 저장영역 대신 `바뀐 어르신이 없습니다. 저장하거나 선택할 것은 없습니다.`로 종료되는 것을 실제 브라우저에서 확인
- 반복 출력이 발생한 OCR 구간만 두 개로 더 나누어 최대 두 단계 재판독하고, 모델이 덧붙인 이미지 설명문은 판독문에서 제외
- 실제 실패 사진 `KakaoTalk_20260722_172641117_01.jpg`를 재판독해 기존 반복오류 없이 `판독 완료 · 확인 필요` 상태로 바뀌는 것을 확인
- 재판독 결과에 실제 글자 오인식과 일부 숫자 잡음은 남아 있어 자동 확정하지 않고 원본 대조가 필요함
- 백엔드 자동시험 12건, 프런트엔드 출력시험 2건, 빌드·정적검사, PostgreSQL 마이그레이션 일치 검사 통과

## 2026-07-27 개발 편의·개인방·전달·알림·음성 판독

- 개발자 전용 런처 로그인 후 재직 가상 사용자 77명의 화면을 직원 비밀번호 없이 전환하고 원래 런처로 복귀
- 전환한 사용자는 본인이 가입한 방만 볼 수 있고 관리자 권한이나 미가입 방 권한을 추가로 받지 않음
- 제출 안전점검에서 `DEV_LAUNCHER_ENABLED=true`를 의도대로 실패 처리하고, 끄기 전에는 제출할 수 없도록 확인
- 기기별 알림소리 `끄기 / 중요 알림만 / 모든 새 메시지` 선택과 브라우저 로컬 저장 확인
- 재직 사용자 77명 모두에게 소유자 전용 `나와의 대화` 방이 생성되고 다른 직원의 직접 접근은 차단
- 메시지 크게 보기에서 현재 가입한 선택 방 또는 모든 가입 방으로 본문·메시지 종류·어르신 연결·첨부파일을 전달
- 전달된 메시지에 원본 방·작성자·메시지를 추적할 수 있는 전달 메타데이터와 화면 배지 표시
- 중지 조직·직종을 관리 화면에서 기본 숨김 처리하고 필요할 때만 펼쳐 복구
- 직원·방·과거 기록에서 참조하지 않는 중지 항목만 완전 삭제, 참조가 남은 항목은 서버 `409`와 `기록 보관`으로 보호
- 기존 Pinokio 설치경로에서 재사용 가능한 `whisper-small` 모델과 RTX 5080 CUDA 실행환경 확인
- 외부 API 전송 없이 로컬 음성 판독 서비스를 별도 프로세스로 실행하고 Docker 백엔드에서 접근
- 실제 Windows 한글 합성음성 WAV 업로드 후 `pending → completed` 자동 받아쓰기와 업무함 `받아쓰기 완료 · 확인 필요` 표시 확인
- 실제 발화 `시설 가명 001 어르신이 점심 식사를 잘 하셨습니다. 간호팀 확인 부탁드립니다.`에서 `가명`이 `감염`으로 판독되는 오류를 확인해 자동 확정하지 않고 원본 대조·수정 저장 흐름 유지
- 브라우저 업무함에서 음성 재생, 받아쓰기 수정, 검토내용 반영, 다시 받아쓰기 버튼 확인
- PostgreSQL·백엔드·프런트엔드·게이트웨이 전체 재시작 전후 `메시지 20 / 방 119 / 개인방 77 / 판독 9`가 동일
- 재시작 후 동일 음성파일의 `completed`, 모델 `whisper-small`, 판독문이 DB와 브라우저 업무함에 유지
- PostgreSQL Alembic `012_audio_transcript_links (head)`, 미반영 모델 변경 없음
- 백엔드 자동시험 `13 passed`, 프런트엔드 출력시험 `2 passed`, 빌드·정적검사 통과

### 검증 한계

- 실제 직원 음성이나 실제 어르신 개인정보는 사용하지 않고 가상 한글 합성음성으로 검증했다.
- 현재 `whisper-small`은 짧은 문장에서도 이름 한 단어를 틀릴 수 있다. 성명·숫자·시간·투약 관련 표현은 담당자가 원본을 듣고 저장해야 한다.
- 브라우저별 실제 운영 알림음은 모바일 운영체제의 절전·무음·브라우저 권한에 영향을 받으므로 물리 휴대전화 시험이 남아 있다.
- 개발자 런처는 개발환경 편의 기능이며 프로토타입 제출본에서는 반드시 비활성화한다.

## 2026-07-27 직종·직위 분리와 자동방 참여자 조정

- 직원 등록·수정 API와 화면에 `직위`를 추가하고 직종과 별도로 PostgreSQL에 저장
- 직종 추가 시 관리자가 영문 내부코드를 입력하지 않아도 서버가 안전한 내부코드를 생성
- 자동 배정방에서도 관리자가 직원 전체 목록에서 참여자를 추가·제외할 수 있도록 구현
- 자동규칙 대상 직원을 제외하면 즉시 방 API 접근 `403`, 다시 포함하면 접근 `200` 확인
- 자동규칙 밖 직원을 직접 추가한 뒤 조직 자동배정 재계산을 실행해도 참여권한이 유지되는 것 확인
- 구성원 변경 시 멤버십만 종료·재생성하고 기존 방 메시지를 삭제하지 않는 구조 확인
- PostgreSQL 마이그레이션 전 custom 형식 백업 생성 후 `013_room_member_overrides` 적용
- Docker 재빌드 후 실제 관리자 화면에서 직원 등록의 `직종·직위` 입력과 `3층 직원방`의 전체 직원 선택목록 확인
- 백엔드 자동시험 `14 passed`, 프런트엔드 출력시험 `2 passed`, 빌드·정적검사 통과

### 검증 한계

- 기존 시험자료에 직종으로 저장된 `대표자·시설장·사무국장`은 실제 자격을 추정할 수 없어 자동 변경하지 않았다.
- 실제 운영 직종·직위 정리는 관리자가 직원별 실제 정보를 확인한 뒤 적용해야 한다.

## 2026-07-27 기존 직종·직위 정리 안내

- 현재 DB에서 직위 성격의 직종으로 배정된 재직자 3명 확인: 시설장 2명, 대표자 1명
- 직원 목록 상단에 `직위로 옮길 직원 3명` 안내와 대상 직원 바로가기 구현
- 대상 직원을 누르면 대표자→대표, 시설장→원장 권장 직위를 입력하되 실제 직종은 관리자가 선택하도록 유지
- 실제 Docker 관리자 화면에서 대상 직원 3명과 가상 원장 직위 `원장` 미리입력 확인
- 직원·활성방·과거배정 참조가 모두 0건인 `사무국장` 과거 직종만 관리자 화면에서 사용중지
- PostgreSQL에서 `office_director.is_active=false`와 `job_code.updated` 감사기록 확인
- 시설장·대표자 직종은 직원·자동방 참조가 남아 있어 중지하거나 삭제하지 않음

### 검증 한계

- 가상 관리자·가상 원장·가상 대표의 실제 자격 직종은 현재 자료로 확정할 수 없어 자동변환하지 않았다.
- 세 직원은 관리자가 실제 직종을 선택해 저장할 때까지 정리대상으로 계속 표시된다.

## 2026-07-27 직위 독립 저장과 과거 직종방 정리

- 마이그레이션 전 PostgreSQL 백업 `data/backups/smcodi_chat_postgres_20260727_163957.dump`와 메타정보 JSON 생성
- `staff.position_title`을 추가하고 기존 현재 배정의 직위를 복사하는 `014_staff_position_title` 적용
- 관리 화면에서 실제 직종을 아직 확인하지 못한 직원은 `미지정(확인 전)`으로 저장할 수 있도록 구현
- 실제 관리자 화면에서 가상 관리자·가상 원장·가상 대표의 현재 직종을 미지정으로 변경하고 직위 `원장·원장·대표`가 유지되는 것 확인
- 직원 저장 API 세 건 모두 `200`, 정리대상 표시가 3명에서 0명으로 감소한 것 확인
- 자동 직종방 재계산 뒤 `시설장방·대표자방` 활성 참여자가 모두 0명인 것 확인
- 관리자 화면에서 두 방을 기록보존 종료하고 `시설장방` 대화 2건이 삭제되지 않은 것 확인
- 참조가 없어진 `시설장·대표자·사무국장` 과거 직종을 사용중지하고 데이터베이스에서 모두 비활성 상태 확인

### 검증 한계

- 세 계정의 실제 자격 직종은 추측하지 않았다. 향후 SMCODI 직원정보 동기화 또는 관리자 확인 후 배정해야 한다.
- 과거 직종 배정과 종료된 방은 감사·대화 보존을 위해 물리 삭제하지 않았다.

## 2026-07-27 운영구조 통합·모바일·복구·배포준비 종합시험

- 최종 전체검증: 백엔드 19개, 프런트 렌더링 2개, 빌드·린트·Alembic 일치 모두 통과
- 변경 전 PostgreSQL 백업 생성 후 직원 층 배정 31건을 같은 이름의 팀 배정으로 이전
- 직원 층 배정 0건, 사업부 78건, 부서 73건, 팀 62건으로 확인
- 운영방을 10개로 정리하고 중복 관리방 29개를 물리 삭제 없이 기록보존 종료
- 운영방 참여자 확인: 전체 78, 시설 37, 주간 8, 방문 31, 2층 5, 3·4·5층 각 6, 영양·복지·의료 15, 복지 5
- 3층 자동방에서 기본 참여자 6명과 관리자 추가·제외 체크 UI·저장 버튼 확인
- 어르신 관리와 케어포 명단 갱신을 한 화면으로 통합하고 시설 가명 어르신 49명·가명 직원 38명 표시 확인
- 과거 연습파일을 불러오는 메뉴와 지난 작업의 연습파일명 숨김
- 390×844 실제 브라우저에서 방 목록·채팅방·사진 미리보기·입력창·알림소리 설정을 확인
- 알림소리를 `모든 새 메시지`로 변경한 뒤 기기 저장값 `all`과 성공 안내 확인
- 서비스워커 제어 활성, 등록 1건, 매니페스트·서비스워커·192 아이콘 HTTP 200 확인
- Caddy 운영 설정 문법검사와 운영 Compose 구성검사 통과
- 운영 안전점검은 정상 시험설정에서 통과하고 `CHANGE_ME` 비밀번호가 남은 예시파일은 의도대로 거부
- DB 백업 `smcodi_chat_postgres_20260727_173617.dump` 생성
- 별도 임시 DB 복원 검증: 직원 78, 방 121, 메시지 21
- DB·백엔드·프런트엔드·게이트웨이 전체 재시작 전후 수치 일치:
  - 재직 직원 78
  - 활성 방 87(관리방·개인방 포함)
  - 메시지 21
  - 활성 어르신 126(케어포·과거 시험연결 포함)
- 재시작 뒤 `/api/health` 정상 응답 확인

### 검증 한계

- 실제 휴대전화의 홈 화면 설치는 `chat.silvermedical.kr` HTTPS 연결 뒤 최종 확인해야 한다.
- 주간보호·방문요양 케어포 명단은 해당 서비스 로그인 세션이 없어 확보하지 못했다.
- 실제 SilverHome 서버 생성, 방화벽, DNS, 인증서 발급은 사용자 지시대로 이번 작업에서 수행하지 않았다.
- 현재 프로토타입 사진은 사용자 결정에 따라 이번 DB 백업에 포함하지 않았다.

## 2026-07-27 기관별 직위 관리·로그인 오류 구분

- 기관별 직위 관리 테이블과 `016_staff_position_catalog` 마이그레이션 적용
- 관리자가 직위를 추가·이름변경·사용중지·복구·완전삭제할 수 있는 API와 `조직정보` 화면 구현
- 재직 직원이 사용 중인 직위는 사용중지할 수 없고, 직위 이름변경 시 해당 직원의 직위도 같이 갱신되는 자동시험 통과
- 등록되지 않은 직위를 직원에게 임의 저장하는 요청은 서버 `422`로 차단
- 실제 관리자 브라우저에서 `브라우저 시험 직위` 추가·노출·사용중지까지 확인 후 시험 항목 물리 삭제
- 직원 등록 화면의 직위를 자유입력 대신 기관 직위 목록에서 선택하는 방식으로 변경
- 개발자 사용자 전환 계정을 일반 직원 API와 관리자 직원 목록에서 제외하고 실제 화면 인원이 `77명 → 76명`으로 정리된 것을 확인
- 잘못된 개발자 런처 비밀번호 입력 시 브라우저 기본 팝업의 `로그인 시간이 만료되었습니다`가 아닌 로그인 화면 안의 `아이디 또는 비밀번호가 올바르지 않습니다.` 표시 확인
- 시험 중 발생한 개발자 런처 로그인 실패기록 1건을 제거하고 잔여 실패기록 0건 확인
- 최종 전체검증: 백엔드 `20 passed`, 프런트 렌더링 `2 passed`, 빌드·린트·Alembic 일치검사 통과

### 검증 한계

- 실제 다른 기관의 직위 명칭은 아직 입력하지 않았지만 기관별 데이터 구조와 관리 화면으로 추가할 수 있다.
- 직위 변경은 채팅방 자동배정에 직접 영향을 주지 않는다. 방 자동배정은 현재와 같이 사업부·부서·팀·직종을 기준으로 한다.

## 2026-07-27 개발자 런처 로그인·복귀 복구

- 현재 개발자 계정이 활성 상태이고 로컬 `.env` 비밀번호와 데이터베이스 비밀번호가 일치하는 것을 확인
- 브라우저에 저장된 과거 비밀번호로 발생한 로그인 실패기록 4건을 제거
- 로컬 개발자 런처 비밀번호를 하나로 다시 맞추고 백엔드 재시작 뒤 비밀번호 일치 확인
- 전환된 사용자 세션은 살아 있지만 개발자 제어 쿠키가 없어진 상황을 실제 브라우저에서 재현
- 유효한 개발자 전환 세션을 근거로 새 제어 세션을 발급하여 로그인 화면을 거치지 않고 런처로 돌아가는 복구 흐름 확인
- 실제 브라우저에서 `개발자 로그인 → 런처 → 관리자 화면 전환 → 제어 쿠키 제거 → 런처 복귀 → 런처 종료` 전 과정 확인
- 최종 전체검증: 백엔드 `21 passed`, 프런트 렌더링 `2 passed`, 빌드·린트·Alembic 일치검사 통과

### 검증 한계

- 개발자 런처 복구는 개발환경에서 런처 기능이 켜져 있고 현재 사용자 세션이 런처에서 발급된 전환 세션일 때만 허용한다.
- 일반 직원 세션이나 운영환경에서는 개발자 런처 제어 세션을 만들 수 없다.

## 2026-07-27 개발자 로그인 저장 비밀번호 오인 방지

- 사용자가 실제 사용 중인 크롬 로그인 화면을 확인한 결과, 아이디는 일치했지만 브라우저가 자동 입력한 비밀번호는 현재 설정값 21자와 다른 23자였음
- 서버 계정·비밀번호 이상이 아니라 크롬에 남은 과거 비밀번호가 반복 입력된 원인으로 확인
- 로그인 화면에 큰 `보기/숨기기` 버튼을 추가하여 저장된 비밀번호가 맞는지 사용자가 직접 확인할 수 있도록 개선
- 개발자 런처 계정에만 복사 과정의 앞뒤 공백을 제거하고, 일반 직원 비밀번호는 기존처럼 입력값을 그대로 검증
- 실제 크롬에서 저장된 23자 값을 지운 뒤 현재 21자 비밀번호로 개발자 런처가 열리는 것을 확인
- 성공 로그인 후 개발자 계정 실패기록이 3건에서 0건으로 정리된 것을 확인
- 개발자 계정 앞뒤 공백 허용 자동시험과 전체 회귀시험 통과

### 검증 한계

- 크롬 비밀번호 관리자에 저장된 과거 값 자체는 프로그램에서 임의 삭제하지 않는다. 로그인 화면의 `보기`로 확인한 뒤 브라우저 저장값은 사용자가 갱신해야 한다.

## 2026-07-27 현재 PC 임시 HTTPS 도메인 연결

- 변경 전 PostgreSQL dump와 첨부파일 압축 백업 생성
- 새 Cloudflare Tunnel `smcodi-chat-office-pc`을 만들고 기존 홈페이지 터널은 변경하지 않음
- `chat.silvermedical.kr` DNS 레코드 자동 생성과 터널 `정상` 상태 확인
- 현재 PC의 Caddy 포트가 `127.0.0.1:8080`에만 열려 있는 것 확인
- 외부 `https://chat.silvermedical.kr/api/health` 응답 `ok`
- 외부 HTTPS 주소에서 관리자 일반 로그인, 채팅방 목록, WebSocket `연결됨` 확인
- 외부 로그인 화면에 개발자 런처 동작이 표시되지 않는 것 확인
- 외부 주소에서 개발자 런처 계정 로그인 요청이 `404`로 차단되는 것 확인
- 응답의 `x-robots-tag: noindex, nofollow, noarchive`, `x-frame-options: DENY`,
  `x-content-type-options: nosniff` 확인
- Windows 로그인 1분 뒤 실행되는 `SMCODI Chat Public Tunnel` 예약 작업 등록
- 백엔드 `22 passed`, 프런트 렌더링 `2 passed`, 빌드·린트·Alembic 일치검사 통과

### 검증 한계

- 실제 휴대전화의 LTE/5G 접속, 홈 화면 설치, 알림소리, PC와의 양방향 메시지 교환은 물리 기기 확인이 남아 있다.
- 현재 PC가 꺼지거나 Docker·인터넷·Cloudflare 커넥터가 중지되면 외부 접속도 중지된다.
- 현재는 개발용 데이터와 런처가 남아 있으므로 실제 개인정보를 입력하거나 운영계정으로 전환하지 않는다.

## 2026-07-28 업무함 AI 연결과 Qwen 35B 비교시험

- 가명 사례 4종: 식사량 관찰, 이동 안전 관찰, 다중 어르신 건강, 보호자 상담
- Qwen 35B 최종 실측:
  - JSON 형식 4/4
  - 원문에 없는 금지 사실 생성 0건
  - 최초 적재 포함 응답 20.22초, 적재 후 4.20~4.58초
  - 분류 3/4, 위험도 4/4, 전달대상 2/4, 서류 후보 3/4
- Gemma 4 비교 실측:
  - JSON 형식 4/4
  - 최초 적재 포함 응답 9.93초, 적재 후 2.12~2.38초
  - 분류·위험도·전달대상·서류 후보 4/4
  - 이동 안전 사례에서 `넘어지지는 않았습니다`라는 핵심 부정 사실 1건 누락
- Qwen 35B를 2순위, Gemma 4를 3순위로 유지하고 이름·수치·시간·부정 표현 보존 지침 강화
- 서버에서 현재 초안보다 위험도를 낮추거나 전달대상·서류 후보를 삭제하지 못하도록 병합 보호
- 전체 자동검증: 백엔드 27개, 프런트 렌더링 2개, 빌드·린트·Alembic 일치 모두 통과
- Docker 재빌드 뒤 실제 가명 업무항목에서 `ollama:qwen3.6:35b`, 11.483초, 담당자 미확정 상태 확인
- 로컬·공개 `/api/health` 모두 `ok`
- 프로젝트 전용 NVIDIA API 키 파일을 Docker 데이터 경로에 연결한 뒤 같은 가명 업무항목을 다시 처리
  - 선택 제공자: `nvidia`
  - 당시 선택 모델: `nvidia/nemotron-3-nano-30b-a3b` (첫 연결검증 기록이며, 현재 기본 모델은 Ultra 550B)
  - 처리시간: 11.898초
  - 시도 순서: Nemotron 1회 성공, Qwen·Gemma 대체 호출 없음
  - 처리 상태: `ai_reviewed`, 담당자 미확정 유지

### 검증 한계

- 위 1건 결과는 첫 연결검증 당시 기록이다. 이후 아래의 제출용 가명 4건 최종시험으로 범위를 확대했다.
- 4개 가명 시나리오는 프로토타입 선별용 소규모 시험이며 장기요양 기록 정확도를 일반화할 수 없다.
- AI 초안은 담당자 확인 전 공식 서류 후보 대시보드에 반영되지 않는다.

## 2026-07-28 제출용 대화 초기화와 AI 가명 4건 최종시험

### 초기화 전 복구자료

- PostgreSQL 전체 백업:
  `data/backups/smcodi_chat_postgres_20260728_084616.dump`
- 백업 확인정보:
  `data/backups/smcodi_chat_postgres_20260728_084616.json`
- 기존 첨부파일 백업:
  `data/backups/smcodi_chat_attachments_20260728_084616.zip`
- PostgreSQL 백업 목차 360줄 확인, 첨부파일 ZIP 18개 항목 열기 확인

### 제출용 데이터 정리 결과

- 기존 메시지 98건, 첨부파일 메타데이터 17건, 업무함 자료 14건을 비움
- 중지 상태의 옛 채팅방 34개를 영구 삭제
- 현재 운영방 10개와 재직자 개인방 77개만 유지
- 운영방: 전체 직원방, 시설 전체방, 주간보호방, 방문요양방, 2층방,
  3층방, 4층방, 5층방, 영양·복지·의료방, 복지방
- 운영 폴더의 과거 첨부파일 17개는 제거했으며 위 ZIP 백업으로 복구 가능
- 직원·조직·어르신·계정·권한·세션 자료는 유지
- 제출용 가명 메시지 22건, 댓글 4건, 업무함 자료 5건을 실제 DB에 생성
- 운영방별 최초 요약 10건을 생성했고 업무함 실제 조회 후 현재 요약행은 14건
- 끊어진 읽음 위치·메시지-방·업무함-메시지 연결은 모두 0건
- 백엔드 재시작 뒤 운영방 10개, 개인방 77개, 메시지 22건, 업무함 자료
  5건이 그대로 유지됨

### AI 최종시험 결과

| 방 / 가명 어르신 | 최종 모델 | 시간 | 분류 | 위험도 | 서류 후보 |
| --- | --- | ---: | --- | --- | --- |
| 3층방 / 시설(가명)003 | Nemotron | 14.503초 | 안전 | 높음 | 간호일지, 통합사정, 급여제공기록 |
| 4층방 / 시설(가명)001 | Nemotron | 11.694초 | 건강 | 중간 | 간호일지, 통합사정, 급여제공기록 |
| 2층방 / 시설(가명)016 | Nemotron | 6.562초 | 영양 | 중간 | 급여제공기록, 통합사정 |
| 주간보호방 / 주간-어르신-01(가명) | Qwen 35B 자동 대체 | 21.010초 | 일상생활 | 중간 | 급여제공기록, 통합사정, 급여제공결과평가, 상담일지 |

- 낙상·피부·영양·인지행동의 장기요양 우선 분류 규칙을 서버 초안과 AI
  안전 병합에 함께 적용
- Nemotron 직접 성공 3건
- 주간보호 인지행동 사례는 Nemotron이 유효한 JSON을 반환하지 못해
  19.961초 뒤 2순위 Qwen 35B로 자동 전환되어 성공
- 모든 AI 결과는 `ai_reviewed`·담당자 미확정 상태로 저장하여 사람이
  원문과 대조한 뒤 확정하도록 유지

### 자동화·실행 검증

- 백엔드 시험 32개 통과
- Alembic 최신 마이그레이션 `017_web_push_subscriptions` 일치
- 새 마이그레이션 필요 없음 확인
- 프런트 렌더링 시험 2개, 빌드, 린트 모두 통과
- AI 수정본으로 백엔드 Docker 이미지 재빌드 및 컨테이너 재생성
- 로컬 `http://127.0.0.1:8080/api/health`와 공개
  `https://chat.silvermedical.kr/api/health` 모두 `ok`
- 공개 브라우저에서 시설 사회복지사 가명 계정에 허용된 방만 표시되고,
  복지방 가명 대화와 업무함 처리자료가 보이는 것을 확인

### 최종 검증 한계

- 가명 4건은 심사 시연용 소규모 검증이며 임상적 정확도나 장기요양
  공식기록 완성도를 보증하지 않는다.
- Nemotron은 유효한 JSON을 반환하지 못할 수 있으므로 Qwen·Gemma·기초
  분류 대체경로와 담당자 확인을 계속 유지해야 한다.
- 실제 개인정보와 공식 서류 자동전송은 이번 제출범위에 포함하지 않는다.

## 2026-07-28 심사위원 시연계정·공개 권한 검증

- 제출 시연용 계정은 3층 요양보호사 작성자, 같은 방 수신자,
  시설 사회복지사 업무함 검토자 3개로 제한
- 세 계정의 기존 세션 12개를 종료하고 과거 기기의 푸시 구독 2개를 해제
- 비밀번호 포함 계정표는 Git 제외 파일
  `data/SUBMISSION_JUDGE_CREDENTIALS.txt`에만 저장
- 관리자 계정과 개발자 런처 계정은 심사위원에게 제공하지 않음
- 공개 `https://chat.silvermedical.kr` 실제 검증:
  - 작성자: 로그인 200, 허용방 4개, 업무함 403, 로그아웃 204
  - 수신자: 로그인 200, 허용방 4개, 업무함 403, 로그아웃 204
  - 검토자: 로그인 200, 허용방 5개, 업무함 200, 로그아웃 204
- 작성자·수신자의 3층방 메시지 조회와 `시설(가명)004` 선택 가능 여부 확인
- 검토자의 권한범위에 해당하는 기존 업무함 가명 사례 4건 조회 확인
  (전체 DB에는 주간보호 사례를 포함해 5건)
- 8분 시연 순서와 실패 대체경로를
  `docs/16_SUBMISSION_DEMO_SCENARIO.md`에 기록

## 2026-07-28 실제 휴대전화 잠금상태 푸시 알림 검증

- 송신: 공개 주소의 보고 작성자 역할, 3층방
- 수신: 실제 안드로이드 휴대전화의 실시간 수신자 역할
- 시험 메시지: `시설(가명)004` 식사량·수분 섭취 후속 보고
- 전송 조작: 09:48:33
- 서버 푸시 성공: 09:48:36
- 서버 실패 횟수: 0회
- 실제 결과: 휴대전화 잠금상태에서 알림음 수신 성공
- 확인된 원인: 브라우저에 남은 예전 푸시 주소와 새 로그인 세션의 서버 등록이
  어긋나면 화면에는 `사용 중`으로 보이지만 시험 알림이 실패했다.
- 수정: 로그인 시 기존 허용 기기의 푸시 주소를 서버에 자동 재등록하고 알림 설정을
  열 때도 등록을 복구한다. 최초 `휴대전화 알림 켜고 시험하기` 한 번으로 등록과
  시험 전송을 함께 수행한다.

## 2026-07-28 OCR 교정 이력 통합 검증

- 실험실 SQLite DB·원본 이미지·개인정보를 복사하지 않고 코드 계약만 참조했다.
- 마이그레이션 `018_ocr_correction_events`를 적용했다.
- 최초 OCR 원문과 검토문이 분리되고 재판독 후에도 최초 원문이 유지됨을 API
  시험으로 확인했다.
- 원문 유지·후보 적용·직접 수정·확인 필요 선택이 이벤트로 추가 저장됨을 확인했다.
- 확인 필요 이벤트는 다음 후보의 학습 근거에서 제외됨을 확인했다.
- 이름·시간·숫자·약·신체 부위·사건 후보가 자동 적용되지 않음을 확인했다.
- 같은 오인의 과거 확정 결과가 후보로 나오며 근거가 늘어도 후보 식별자가
  유지됨을 확인했다.
- 실제 브라우저에서는 운영 DB를 바꾸지 않는 가명 임시 응답을 사용해 최초 원문,
  교정 후보, 중요 항목 경고, 네 가지 선택과 후보 선택 후 교정문 변경을 확인했다.
- 전체 회귀시험은 백엔드 36개, Alembic 최신 상태·변경 없음, 프런트엔드
  빌드·2개 렌더링 시험·린트가 모두 통과했다.

## 2026-07-28 업무함 당일서류 통합 검증

- 백엔드 시험 41개, 프런트엔드 렌더링 시험 2개, 린트, 운영 빌드가 통과했다.
- Alembic 현재 버전은 `020_document_draft_index_names`이며 추가 변경이 필요 없음을 확인했다.
- 로컬 `http://127.0.0.1:8080/api/health`와 공개
  `https://chat.silvermedical.kr/api/health`가 모두 `ok`를 반환했다.
- 실제 PC 브라우저에서 저위험 가명 사례의 AI 초안을 수정한 뒤 최종 버튼 한 번으로
  업무내용과 선택 서류를 승인했다. 서류 초안 버전 2와 승인본 출력 버튼을 확인했다.
- 실제 PC 브라우저에서 낙상 직전 고위험 가명 사례를 AI로 다시 정리했다.
  간호일지와 급여제공기록지 초안이 함께 표시됐고, 확인질문 응답 전에는 최종 승인
  버튼이 비활성화되며 응답 후 활성화됨을 확인했다. 해당 사례는 시험상 최종 승인하지 않았다.
- Nemotron이 당일서류 초안 또는 고위험 확인질문을 빠뜨려도 서버 기초규칙 결과와
  병합해 화면에서 사라지지 않음을 자동시험과 브라우저에서 확인했다.
- 모바일 390×844 화면에서 업무함 탭 4개가 모두 보이고 서류 초안 카드가 세로로
  배치되며 가로 넘침이 없음을 확인했다.
- 요양보호사의 채팅 전송은 AI 호출과 분리되어 즉시 처리된다. Nemotron 재정리는
  담당자 업무함에서 약 20~30초가 걸릴 수 있다.
- 신체제재 기록지와 프로그램 운영기록지 PDF는 실제 개인정보를 저장소에 복사하지
  않고 필드 구조만 확인해 초안 항목과 확인질문 설계에 반영했다.
- 실제 SMCODI 입력화면·데이터베이스·API는 수정하거나 호출하지 않았다. 현재 결과는
  가명 프로토타입의 승인 초안이며 공식 서류 자동등록은 검증 범위 밖이다.

## 2026-07-28 업무함·업무전달·대화검색 단순화 검증

- 백엔드 전체 시험 43개, 프런트엔드 렌더링 시험 2개, 린트와 운영 빌드가
  모두 통과했다.
- Alembic 현재 버전은 `020_document_draft_index_names`이며 모델과 DB 사이에
  새 마이그레이션 누락이 없음을 확인했다.
- 채팅 입력창에는 어르신 선택·파일·공지·전송만 남고, 업무전달은 메시지를 크게
  연 뒤 담당자·중요도를 지정하는 한 흐름으로 이동한 것을 공개 브라우저에서 확인했다.
- 업무함의 기존 4개 기능 탭을 제거하고 `검토 필요·승인 완료·사용 안 함·전체`
  상태버튼과 한 개의 검토화면만 표시되는 것을 확인했다.
- 상담일지와 급여제공기록지가 각각 상담·연락 결과, 관찰·지원·반응에 맞는 서로
  다른 초안으로 생성되는 것을 자동시험과 공개 브라우저에서 확인했다.
- 승인 취소 후 재검토 상태로 돌아가며 기존 승인본과 버전 이력은 남는 것을
  자동시험으로 확인했다.
- 대화방 검색에서 기간·단어·어르신·메시지 종류·업무상태를 조합할 수 있고,
  댓글·OCR 검토문·음성 받아쓰기까지 검색되는 것을 확인했다.
- AI 대화요약은 검색 결과 번호만 근거로 표시하며 존재하지 않는 번호를 모델이
  반환하면 `근거 확인 필요`로 바꾸도록 보호했다.
- Docker 재빌드 후 로컬·공개 `/api/health` 응답이 모두 `ok`였고,
  공개 `https://chat.silvermedical.kr`에서 검색·AI 요약·업무함·업무전달
  화면을 확인했다.
- 모바일 390×844 화면에서는 상태버튼 4개가 한 줄에 모두 보이고 자료목록,
  원문 보존 안내, 연결 어르신 변경, 서류 초안이 한 열로 이어지며 가로 잘림이
  없음을 화면 캡처로 확인했다.
- 공개 브라우저 검증 중 실제 업무전달이나 최종 승인은 누르지 않아 운영 가명자료의
  처리상태를 변경하지 않았다.

## 2026-07-28 기간별 업무대화 정리·미디어 검증

- 업무함을 개별 대화 한 건씩 처리하는 방식에서 `기간별 업무대화 정리` 방식으로
  변경했다. 기간·검색어·대화 성격·채팅방·어르신을 선택하면 해당 근거 대화,
  AI 기간 요약, 일일 작성 서류 초안을 한 화면에서 확인할 수 있다.
- 채팅 입력창의 선택 기능을 `일반 대화·인수인계·업무협조·보고`로 단순화했다.
  선택하지 않으면 일반 대화로 저장되며 담당자·중요도를 먼저 지정하지 않아도 된다.
- 업무대화 상세에서 실제 읽은 직원 수, 답글을 작성한 직원 수, 전체 답글 수를
  구분해 표시하는 것을 확인했다.
- 기간별 근거 대화에 이미지·음성·동영상 첨부 미리보기와 재생기를 연결했다.
  이미지는 클릭하면 전체화면으로 열리고 터치 확대·축소와 두 번 누르기 초기화를
  지원한다.
- MP4·WebM·MOV 동영상 업로드와 브라우저 내 재생 구조를 추가했다. 파일당 최대
  30MB, 메시지당 최대 4개 제한은 기존 첨부파일 정책을 따른다.
- 여러 어르신이 함께 언급된 음성·OCR 판독문을 한 어르신의 서류 초안에 전부
  복사하지 않도록 어르신별 근거문장을 분리했다. `시설 018`처럼 괄호가 생략된
  가명 표기도 같은 어르신으로 식별하는 자동시험을 추가했다.
- 백엔드 전체 시험 44개, 프런트엔드 렌더링 시험 2개, 린트, 운영 빌드가
  모두 통과했다.
- Alembic 현재 버전은 `020_document_draft_index_names`이며 새 마이그레이션
  작업이 없음을 확인했다.
- Docker 백엔드·프런트엔드·게이트웨이를 다시 빌드·시작한 뒤 로컬
  `http://127.0.0.1:8080/api/health`와 공개
  `https://chat.silvermedical.kr/api/health`가 모두 `ok`를 반환했다.
- PC 930×930과 모바일 390×844 화면에서 기간별 필터·요약·근거 대화가 가로로
  넘치지 않는 것을 확인했다. 모바일 상세화면 닫기 버튼도 개발자 시험 표시줄에
  가려지지 않도록 보완했다.
- 공개 브라우저 콘솔 오류는 0건이었다. 실제 운영자료의 상태를 바꾸지 않기 위해
  새 동영상은 공개 채팅방에 올리지 않았으며, 실제 휴대전화의 두 손가락 확대와
  동영상 업로드·재생은 사용자가 보유한 시험파일로 최종 확인이 필요하다.

## 2026-07-28 판독문 확인·기간 정리 속도·인쇄·검색창 검증

- 이미지 OCR과 음성 받아쓰기 결과를 `업무대화 상세`의 각 첨부파일 아래에
  원본 파일과 구분해 표시했다. 판독 중에는 3초 간격으로 상태를 다시 확인하므로
  상세창을 닫았다 열지 않아도 완료된 텍스트가 나타난다.
- 업무함 처리 권한이 있는 직원뿐 아니라 원래 첨부한 직원도 자신이 참여 중인
  채팅방에서 자기 첨부파일의 판독문을 직접 수정·저장할 수 있음을 API 시험과
  모바일 390×844 브라우저 화면으로 확인했다. 다른 일반 직원의 첨부 판독문은
  수정할 수 없다.
- 기간별 업무대화 정리는 대화 근거와 일지 초안을 먼저 만드는 빠른 단계와
  AI 요약을 뒤에서 보완하는 단계로 분리했다. 실제 PC 브라우저에서 정리 버튼
  동작 뒤 약 2.9초 안에 대화·일지 화면이 먼저 표시됐고, 상단에
  `AI 요약 다듬는 중` 상태가 별도로 표시됐다.
- 인쇄는 현재 화면 일부를 그대로 인쇄하지 않고 선택한 일지만 담은 별도 A4
  인쇄 문서를 생성하도록 변경했다. 일지마다 새 페이지에서 시작하며 내용이 긴
  경우 다음 페이지로 자연스럽게 이어지도록 자동시험으로 확인했다.
- 대화 검색창은 검색 조건을 입력할 때마다 자동 재검색하지 않고 `검색` 버튼이나
  Enter 키로 실행하도록 변경했다. 모바일에서는 검색 전·후 모두
  390×844, 내부 너비 388px를 유지했고, PC에서는 검색 결과 유무와 관계없이
  1040×820 크기를 유지했으며 가로 넘침이 없었다.
- `scripts/verify.ps1` 결과 백엔드 44개, 프런트엔드 렌더링 2개, 린트,
  운영 빌드, Alembic 변경 없음 검사가 모두 통과했다.
- Docker 백엔드·프런트엔드·게이트웨이를 재빌드한 뒤 로컬과 공개
  `/api/health`가 모두 `ok`를 반환했다.
- Windows 인쇄 미리보기의 실제 용지·여백 선택과 물리 프린터 출력은 자동화
  범위 밖이므로 사용자가 브라우저 인쇄 미리보기에서 최종 한 번 확인해야 한다.

## 2026-07-28 오늘의 돌봄 브리핑 검증

- 업무함 첫 화면을 `오늘의 돌봄 브리핑`으로 바꾸고 어르신별로
  `무엇이 달라졌는지·왜 확인해야 하는지·이미 한 일·아직 확인할 일·만들 수
  있는 기록`을 한 카드에서 확인하도록 구성했다.
- 선택 기간 직전 3일의 같은 어르신 기록을 비교 근거로 사용한다. 비교기록이
  없을 때는 변화로 단정하지 않고 `오늘 관찰을 첫 기준으로 표시`한다고
  명시한다.
- 안전사고·출혈 등 긴급 확인 표현, 평소와 다른 상태, 담당 업무의 미완료,
  원문 확인 질문을 먼저 확인 순서에 반영한다. 이 순서는 공식 위험평가 점수나
  의학적 판단이 아니다.
- 한 문장에 상태 변화와 시행 조치가 함께 적힌 경우에도 비틀거림·발적·혈압·
  식사·배변·수면 등의 상태 표현을 변화 요약에서 먼저 보존하고, 부축·전달·
  체위변경·교체 등은 `이미 한 일`에도 별도로 표시하도록 보정했다.
- 브리핑의 `근거 보기`는 해당 원문 대화를 펼치고, `기록 초안 보기`는 같은
  어르신의 일일서류 초안으로 이동하도록 연결했다.
- PC 1440×1000과 모바일 390×844 실제 브라우저에서 첫 카드 3명, 지표 5개,
  네 가지 핵심 질문, 근거·기록 버튼이 가로 넘침 없이 표시되는 것을 확인했다.
- 로컬과 공개 `/api/health`가 모두 `ok`를 반환했다. 공개 주소는 현재 PC의
  Cloudflare Tunnel을 통해 같은 Docker 실행본을 제공한다.
- 전체 자동검사 결과는 백엔드 44개, 프런트엔드 렌더링 2개, 린트, 운영 빌드,
  Alembic 변경 없음이 모두 통과했다.
- 실제 데이터에 직전 3일 비교기록이 없는 어르신은 첫 기준으로 표시된다.
  장기간의 `평소와 다름` 정확도는 가명 기록을 누적해 별도로 검증해야 한다.

## 2026-07-29 돌봄 브리핑 최종 단순화·기록별 모아보기 검증

- 기존 채팅 원문·댓글·읽음·OCR/음성 판독문·어르신 연결·AI 분석·승인 이력을
  삭제하지 않고, 반복되던 큰 일지 초안 목록만 첫 화면에서 숨겼다.
- 2026-07-28 공개 가명자료를 실제 조회한 결과 수집 메시지 23건을 어르신별
  사건 44건으로 분리했다. 손글씨 한 장에 여러 어르신이 있는 경우 같은 원문을
  근거로 연결하되 각 어르신 사건은 분리했다.
- 같은 사건은 한 카드에서 한 번만 보여주고 간호 16건, 급여제공 34건,
  보호자상담 3건, 프로그램 2건, 일반업무 6건으로 다시 모아보는 것을 확인했다.
  한 사건이 둘 이상의 기록 근거가 될 수 있으므로 분류별 건수 합계는 전체 사건
  수와 같지 않다.
- 시설(가명)012 비틀거림은 간호·급여제공으로 분류되고 보호자상담은 붙지 않았다.
  시설(가명)027의 보호자를 찾는 표현은 실제 연락이 없어 상담으로 분류되지 않았다.
  시설(가명)007의 실제 보호자 통화는 상담, 시설(가명)008의 실제 참여는 프로그램,
  시설(가명)036의 혈압 재측정은 간호로 분류됐다.
- 어르신과 직접 관련 없는 간호 공지 문장은 간호가 아닌 일반업무에만 포함되도록
  회귀시험을 추가했다. 이름·시간·수치·약·신체 부위가 불확실하면 자동 확정하지
  않고 추가확인 대상으로 남기는 기존 보호규칙을 유지했다.
- 간호 관련 사건에서 시설(가명)012 원문 1건만 선택해 공개 Nemotron AI 정리를
  실행했다. 약 27초 후 주요 내용·후속 확인·완료 조치가 근거 `[1]`과 함께
  생성됐고, 원문에 없는 진단·평가점수는 추가되지 않았다.
- PC 1440×1000과 모바일 390×844 공개 화면을 확인했다. 모바일의 창·본문
  `scrollWidth`와 `clientWidth`가 모두 390px로 일치해 가로 넘침이 없었다.
- 공개 브리핑을 독립 A4 PDF로 저장한 결과 17페이지가 생성됐다.
  첫 페이지·9페이지·17페이지를 PNG로 렌더링해 본문 잘림, 겹침, 스크롤 영역
  누락이 없고 한글·출력기간·페이지 번호가 정상임을 육안 확인했다.
- `scripts/verify.ps1` 결과 백엔드 53개, Alembic 최신
  `020_document_draft_index_names`·추가 변경 없음, 프런트엔드 빌드·렌더링
  시험 2개·린트가 모두 통과했다.
- 공개 `https://chat.silvermedical.kr/api/health`는 `ok`, 서비스워커는
  `mesil-chat-shell-v8`, 브라우저 콘솔은 오류 0건·경고 0건이었다.
- AI 정리는 사용자가 근거를 선택할 때만 실행된다. 현재 약 27초의 외부 AI 응답
  대기시간은 제출 프로토타입에서 허용 가능한 범위로 확인했지만, 운영 전에는
  비동기 작업 상태와 재시도 정책을 추가 검증해야 한다.

## 2026-07-29 기록 목적 복수 선택·업무 브리핑 검증

- 간호·급여제공·상담·프로그램·일반업무·확인필요를 체크 방식으로 일부 또는
  복수 선택하고, `모두 선택`과 `선택 해제`로 한 번에 바꾸는 화면을 구현했다.
- 선택한 여러 분류의 사건을 중복 없이 합쳐 같은 AI 정리 요청에 전달하고,
  응답에도 선택한 기록 목적 목록이 유지되는 회귀시험을 추가했다.
- 기존의 원문 누락 금지 검사가 함축 요약까지 실패로 처리해 장문 원문 모음으로
  대체하던 원인을 수정했다. 이제 함축은 허용하되 원문에 없는 어르신·수치·시간·
  위험 사실은 계속 차단한다.
- 가명 사례 4건을 실제 Nemotron으로 호출한 결과 약 1.5초에 `한눈에 보기`,
  `먼저 확인`, `이미 한 일`, `다음 업무 제안`이 근거번호와 함께 생성됐다.
  AI 연결이 실패해도 장문 원문 나열이 아니라 같은 네 구역의 안전 브리핑으로
  대체된다.
- 전체 검증 결과 백엔드 65개, Alembic 최신
  `020_document_draft_index_names`·추가 변경 없음, 프런트엔드 빌드·렌더링
  시험 2개·린트가 모두 통과했다.

## 2026-07-30 심사위원 전용 역할 체험 검증 결과

심사위원에게 일반 직원 계정의 아이디·비밀번호를 직접 전달하지 않고
`https://chat.silvermedical.kr/reviewer`에서 역할을 선택하게 하는 기간제
체험 흐름을 검증한다. 일반 직원 로그인 주소 `/`와 기존 로그인 방식은 그대로
유지한다.

### 자동검사

- 백엔드 94건, Alembic 최신
  `020_document_draft_index_names`·추가 변경 없음, 프런트엔드 운영 빌드,
  렌더링 시험 3건, 린트가 모두 통과했다.
- 기간제 심사 세션, 보안 쿠키, 재사용·역할 전환·만료·수동 비활성화,
  WebSocket 강제 종료, 관리자·계정·푸시 기능 차단을 자동검사했다.
- 심사방·계정·참여자·메시지·연결 어르신의 시험자료 여부를 세션 발급 전에
  검사하며, 지정방 밖 메시지와 비시험자료가 결과에 섞이지 않는 것을 확인했다.

### 공개 API

- `https://chat.silvermedical.kr/api/health`는 `ok`,
  `/reviewer`는 200, 공개 `/api/docs`는 404를 반환했다.
- 요양보호사 역할은 지정 가명방 1개와 메시지 26건만 조회했다. 다른 방,
  관리자 API, 업무함 어르신 API는 모두 403으로 거부됐다.
- 사회복지사 역할은 AI 돌봄 브리핑으로 이동해 최신 가명 시연일
  2026년 7월 29일의 근거 대화 10건을 불러왔다. 기존 원본 업무함 API는
  403으로 거부됐다.
- 역할 선택 응답과 공개 HTML에서 서버에 설정된 실제 내부 계정 식별자가
  노출되지 않았다.

### 실제 브라우저

- PC 1440×900과 모바일 390×844에서 `/reviewer`의 두 기본 역할 버튼이
  모두 보이고 `scrollWidth`가 화면 너비와 같아 가로 넘침이 없었다.
- 요양보호사 화면은 지정방 1개, 가명 첨부자료, 읽음·답글 표시를 보여주며
  관리자·업무함 메뉴는 표시하지 않았다.
- 사회복지사 화면은 별도 조작 없이 2026년 7월 29일 AI 돌봄 브리핑을 열고
  우선 확인 어르신과 원문 근거를 표시했다.
- 두 역할 모두 새로고침 후 심사 상태와 지정 화면이 유지됐다.
- 브라우저 콘솔의 유일한 오류는 Cloudflare 통계 스크립트가 서비스
  콘텐츠 보안정책에 의해 차단된 메시지였다. 앱 기능 오류는 확인되지 않았다.

### 운영 경계

- 제출폼에는 비밀번호를 적지 않고 심사위원 전용 주소만 안내한다.
- 심사 종료 또는 실제 개인정보 운영 전 `REVIEWER_ACCESS_ENABLED=false`로
  전환해 신규 심사 세션을 차단한다.

## 2026-07-30 심사 체험 반복 전환 회귀시험

### 발견한 원인

- 역할 체험을 열 때마다 새 로그인 세션을 만들고, 역할 선택 화면으로 돌아갈 때
  폐기한 세션도 15분 동안 발급 횟수에 포함됐다.
- 발급 제한이 브라우저가 아닌 공개 IP 기준이라 같은 기관망의 여러 심사위원이
  제한 횟수를 함께 사용했다.

### 수정 후 검증

- 같은 브라우저에서 요양보호사·사회복지사 역할을 공개 주소로 12회 연속
  전환했고 모든 요청과 사용자 확인 API가 200으로 성공했다.
- 같은 공개 IP의 서로 다른 브라우저 세션 2개를 동시에 열어도 각각 정상적으로
  체험을 시작하고 유지했다.
- 역할을 바꿔도 기존 심사 세션 행을 재사용하고 토큰만 안전하게 교체하며,
  이전 토큰은 즉시 무효가 되는 것을 자동검사했다.
- 역할 선택 화면으로 돌아가는 동작은 세션을 유지하고, `직원 로그인으로
  돌아가기`를 누른 경우에만 심사 세션을 종료하는 것을 확인했다.
- 신규 익명 브라우저의 과도한 세션 발급은 여전히 429로 차단된다. 제한 창은
  8회/1분이며 브라우저별 익명 식별값으로 분리된다.
- 전체 자동검사 결과 백엔드 94건, 마이그레이션 검사, 프런트 운영 빌드,
  렌더링 시험 3건, 린트가 모두 통과했다.
- 재배포 후 로컬·공개 `/api/health` 응답이 모두 `ok`였고, PC 실제 브라우저와
  모바일 390×844 화면에서 반복 역할 전환과 가로 넘침이 없는 것을 확인했다.

## 2026-07-30 메딕 음성 태그·대화 입력창 검증

### 알림음

- 제공받은 `08_medic_voice_tag.wav`를
  `/sounds/mesil-medic-voice-v2.wav`로 추가했다.
- 원본과 배포 파일의 SHA-256 해시가
  `B1ADD9F2F2BA2C06B1E914E2CFDE46EE8F3BACAC3D6B56A23CBABE8361416CD8`로
  일치했고, 파일 크기 78,764바이트·48kHz 모노·약 0.82초임을 확인했다.
- 전면 앱·PC 알림과 시험 알림이 새 파일을 사용하며, 서비스워커 캐시를
  `mesil-chat-shell-v10`으로 올려 기존 음원이 남지 않게 했다.
- 로컬·공개 주소에서 새 음원과 서비스워커가 200으로 응답했고, 공개
  서비스워커에 새 음원 경로가 포함되고 이전 경로는 없음을 확인했다.
- 잠금화면 Web Push 알림음은 아이폰·안드로이드 시스템 설정을 따르므로
  이 음원으로 강제 변경되지 않는다.

### 대화 입력창

- `일반 대화` 선택 상자의 바깥 라벨 테두리를 제거하고 실제 선택 상자 하나만
  표시되도록 정리했다.
- 공개 실제 브라우저에서 바깥 라벨은 테두리 0·패딩 0, 안쪽 선택 상자는
  130×40픽셀·테두리 1개로 계산됐다.
- PC 1440×900과 모바일 390×844에서 `어르신 선택`, `파일`, `일반 대화`,
  메시지 입력, `보내기`가 가로 잘림 없이 표시됐다.

### 회귀시험

- 백엔드 94건, Alembic 최신·추가 변경 없음, 프런트 운영 빌드,
  렌더링 시험 3건, 린트가 모두 통과했다.
- 공개 브라우저 콘솔의 유일한 오류는 기존과 같은 Cloudflare 통계 스크립트
  차단 메시지였으며 앱 기능 오류는 확인되지 않았다.
